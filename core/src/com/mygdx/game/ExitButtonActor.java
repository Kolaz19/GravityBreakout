package com.mygdx.game;

import com.badlogic.gdx.graphics.g2d.Batch;

public class ExitButtonActor extends ButtonActor {

    public ExitButtonActor(String pathButtonDefault, String pathButtonPressed, int xCord, int yCord) {
        super(pathButtonDefault, pathButtonPressed, xCord, yCord);
    }

    @Override
    public void draw(Batch batch, float parentAlpha) {
        super.draw(batch, parentAlpha);
    }

    @Override
    public void act(float delta) {
        super.act(delta);
    }

}
